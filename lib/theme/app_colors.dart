import 'package:flutter/material.dart';

class AppColors {
  // Colores principales de la app
  static const Color primary = Color.fromARGB(255, 1, 165, 165); // Teal
  static const Color secondary = Color.fromARGB(255, 0, 62, 51); // Verde oscuro
  static const Color third = Color.fromARGB(255, 60, 115, 255); // Azul 
  static const Color accent = Color.fromARGB(255, 127, 175, 168); // Verde menta
  static const Color fourth = Color.fromARGB(255, 255, 68, 102); //Purpura 
  static const Color fifth = Color.fromARGB(255, 235, 200, 1); //Amarillo
  static const Color sixth = Color.fromARGB(255, 199, 113, 0);  //Anaranjado
  static const Color seventh = Color.fromARGB(255, 66, 187, 72);  //Verde 

  // Colores de fondo
  static const Color backgroundback = Color.fromARGB(255, 0, 0, 0);
  static const Color background = Color.fromARGB(255, 181, 181, 181); // Gris claro
  static const Color cardBackground = Color(0xFFFFFFFF); // Blanco

  // Colores de texto
  static const Color textPrimary = Color(0xFF212121); // Negro oscuro
  static const Color textSecondary = Color(0xFF757575); // Gris oscuro

  // Colores de error
  static const Color error = Color(0xFFD32F2F); // Rojo
}
